node_modules
dist
coverage
.turbo
*.log
.env
.env.*
!.env.example

# Samples are user-provided — never committed
samples/**/*
!samples/**/.gitkeep
!samples/README.md

# Sounds are user-provided — never committed
sounds/**/*
!sounds/**/.gitkeep
!sounds/README.md

# Claude Code local state
.claude/

# Bug reports written by the app — not committed
debug/

# Local only — not ready to publish
THESIS.md
mcp-servers/score-codebase/node_modules/
